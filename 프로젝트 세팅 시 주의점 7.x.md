## 프로젝트 7.x 세팅시 주의
- setup 이전에 프로젝트를 7.x로 세팅할 때 주의해야 할 점들을 정리해보았습니다. 
1. setup 이전에 elasticsearch/config/elasticsearch.yml 파일의 xpack.license.self_generated.type: basic 으로 변경해야함
2. elastic search 에서    
    ```curl -X POST "localhost:9200/_license/start_basic?acknowledge=true" -H "Content-Type: application/json" -u elastic:<your current elastic password> ``` 를 통하여
    라이센스를 basic으로 변경해야함
